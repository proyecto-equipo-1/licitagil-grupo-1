// import { Amplify } from 'aws-amplify';
// import outputs from '../../amplify_outputs.json';

// Amplify.configure(outputs);

// Temporary placeholder until amplify_outputs.json is generated
console.log('Amplify configuration temporarily disabled for build');

export default {};