version: 1
backend:
  phases:
    preBuild:
      commands:
        - echo "Installing backend dependencies..."
        - cd api
        - npm ci
        - echo "Generating Prisma Client..."
        - npx prisma generate
    build:
      commands:
        - echo "Building backend..."
        - npm run build
        - echo "Running database migrations (if DATABASE_URL is set)..."
        - '[ -n "$DATABASE_URL" ] && npx prisma migrate deploy || echo "Skipping migrations: DATABASE_URL not set"'
        - echo "Creating templates directory..."
        - mkdir -p dist/templates
        - echo "Copying template PDFs..."
        - cp templates/*.pdf dist/templates/ || echo "No template PDFs found"
    post_build:
      commands:
        - echo "Backend build complete"
  artifacts:
    baseDirectory: api/dist
    files:
      - '**/*'
      - templates/**/*
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing frontend dependencies..."
        - cd web
        - npm ci
        - npm install aws-amplify
    build:
      commands:
        - echo "Building React application..."
        - echo "VITE_API_URL=$VITE_API_URL" > .env.production
        - npm run build -- --mode production
  artifacts:
    baseDirectory: web/dist
    files:
      - '**/*'
  cache:
    paths:
      - web/node_modules/**/*
      - api/node_modules/**/*